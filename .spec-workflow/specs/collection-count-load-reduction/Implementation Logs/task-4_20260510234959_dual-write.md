# Task 4 Implementation Log: Dual-Write Collection Events

## Summary

Updated the shared collection events backfill/sync/rescan path so production writes now dual-write to `collection_events`, `collection_count_event_existence_log`, and `collection_count_ingest_queue` before checkpoint advance. Added bounded raw bootstrap support that reads existing `collection_events` and writes only the collection count sidecar tables.

## Files

- Modified `packages/clickhouse-tools/src/backfill-collection-events.ts`
- Modified `packages/clickhouse-tools/src/backfill-collection-events.test.ts`
- Updated task status in `.spec-workflow/specs/collection-count-load-reduction/tasks.md`

## Verification

- `node --test packages/clickhouse-tools/src/backfill-collection-events.test.ts`
- `pnpm --filter @atpdashboard/clickhouse-tools test`

## Artifacts

- `buildCollectionEventDualWriteRows`: builds raw event rows, existence log rows, and queue rows with shared `payload_hash`, non-empty `queue_seq`, and cutoff bumping.
- `buildCollectionCountSidecarRows`: builds queue/existence rows from existing raw `collection_events` without reinserting raw rows.
- `--bootstrap-queue-from-raw`: bounded bootstrap mode for existing `collection_events` sidecar backfill.
- `runBackfill`: checkpoint updates now occur only after raw, existence log, and queue writes complete; queue-write failure after raw insert leaves checkpoint unchanged for retry.
